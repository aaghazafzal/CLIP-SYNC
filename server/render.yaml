services:
  - type: web
    name: clipsync-relay
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
